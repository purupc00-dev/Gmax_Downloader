{
  "name": "gmax-downloader",
  "version": "1.0.0",
  "description": "Standalone downloader API + UI for GmaxHub. Resolves TMDB IDs to direct stream URLs via embed-player scraping. No file storage — returns source URLs to the client.",
  "type": "module",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "cheerio": "^1.0.0",
    "compression": "^1.7.5",
    "cors": "^2.8.5",
    "express": "^4.21.2",
    "express-rate-limit": "^7.4.1",
    "node-cache": "^5.1.2",
    "undici": "^7.1.0"
  }
}
